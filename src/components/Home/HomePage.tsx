'use client';
import { FC } from 'react';
import { HomeCarousel } from './HomeCarousel';

export const HomePage: FC = () => {
    return (
        <>
            <HomeCarousel />
        </>
    );
};
